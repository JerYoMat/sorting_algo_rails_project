<% provide(:title, 'User Index')%>

<h1>User Index</h1>

<ul class="users">
  <% @users.each do |user|%>
    <li ><%= link_to(user.name, user)%><span>Joined <%= user.created_at.strftime('%B %Y')%></span></li>
  <% end %>
</ul>